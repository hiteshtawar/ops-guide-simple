# Local development profile - Security completely disabled for easier testing
server:
  port: 8093

downstream-services:
  services:
    ap-services:
      base-url: https://dev-api.example.com/v2
      timeout: 30

logging:
  level:
    com.lca.productionsupport: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG

