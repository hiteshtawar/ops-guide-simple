# Development profile
spring:
  application:
    name: ops-guide-simple-dev

server:
  port: 8093

case-management:
  api:
    base-url: https://dev-api.example.com/v2

logging:
  level:
    com.productionsupport: DEBUG
    org.springframework.web: DEBUG

